@model Afilhado4Patas.Models.ViewModels.AnimalFileUploadViewModel

@using Microsoft.AspNetCore.Identity
@using Afilhado4Patas.Data;

@inject SignInManager<Utilizadores> SignInManager
@inject UserManager<Utilizadores> UserManager
@inject ApplicationDbContext Context
@{
    ViewData["Title"] = "Adicionar Anexos Animal";
}


<div id="page">
    <div id="box">
        @{
            if (Model.animal.Foto != null)
            {
                string src = "/Animais/" + Model.animal.Id + "/" + Model.animal.Foto;
                <img id="icon_centrado_arredondado" src="@src" />
            }
            else
            {
                <img id="icon_centrado_arredondado" src="~/images/dog-profile.png" />
            }
        }
        <h4>Adicionar Anexos á ficha do Animal </h4>
        <hr />
        <form method="post" asp-action="AdicionarAnexosFicheirosAnimal" asp-controller="Responsavel" enctype="multipart/form-data">
            <div class="form-group">
                <label asp-for="File" class="control-label"></label>
                <span data-placement="right" class="glyphicon glyphicon-info-sign" style="color:#539be2"
                      title="Insira um ficheiro.
Deverá ter o formato PDF."></span>
                <input type="file" asp-for="File" class="form-control" />
                <span asp-validation-for="File" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Confirmar" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="Responsavel" asp-action="DetalhesAnimal" asp-route-id=@Model.animal.Id>Voltar Ficha Animal</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}